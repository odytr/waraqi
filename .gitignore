# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies and build output
node_modules/
dist/
dist-ssr/
*.local
src-tauri/target/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local test images — not ours to publish
test-set/

# Regenerated by Tauri on every build, pure diff noise
src-tauri/gen/schemas/

# Secrets, in case any ever show up
.env
.env.*

# OS junk
Thumbs.db
desktop.ini

# Built installers, if any land outside src-tauri/target
*.msi
*.exe

# NOTE: public/ is committed on purpose. Those tessdata, tesseract cores and
# the Amiri font are what make the offline claim true. Anyone who clones this
# repo gets a working build without downloading anything.

log.md
